{
  "name": "@zerve/cli",
  "version": "0.1.0-alpha.4",
  "license": "Apache-2.0",
  "scripts": {
    "prepare": "yarn build",
    "build": "esbuild ./ZerveSync --bundle --platform=node --outdir=dist && chmod +x ./dist/ZerveSync.js",
    "clean": "rimraf dist node_modules"
  },
  "bin": {
    "zerve-sync": "./dist/ZerveSync.js"
  },
  "dependencies": {
    "node-fetch": "^3.2.10",
    "fs-extra": "^10.1.0",
    "json-schema-to-typescript": "^11.0.2"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2",
    "@types/fs-extra": "^9.0.13"
  },
  "files": [
    "dist"
  ]
}
