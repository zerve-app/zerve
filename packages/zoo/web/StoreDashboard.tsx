import { DashboardPage } from "./Dashboard";

type StoreNavigationState = {};
type StoreFeatureSpec = {};

export function StoreDashboard({
  storeId,
  entityId,
}: {
  storeId: string;
  entityId: string;
}) {
  return null;
}
